<UserControl x:Name="userControl" x:Class="Speak2Score.ButtonArray"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Speak2Score"
             mc:Ignorable="d"
             d:DesignHeight="140" d:DesignWidth="240" Width="{Binding Width, ElementName=wpButtonArray}" Height="{Binding Height, ElementName=wpButtonArray}">
    <WrapPanel Name="wrappanel"
                Width="{Binding dynamicWidth, ElementName=userControl}" Height="90"
                Orientation="Vertical">

        <WrapPanel.Resources>
            <Style TargetType="Button">
                <Setter Property="Width" Value="24" />
                <Setter Property="Height" Value="18" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="HorizontalAlignment" Value="Left" />
                <!--<Setter Property="VerticalAlignment" Value="Top" />-->
                <Setter Property="BorderBrush" Value="Transparent" />
            </Style>
        </WrapPanel.Resources>
    </WrapPanel>
</UserControl>